fastapi>=0.104.0
uvicorn>=0.24.0
requests>=2.31.0
python-dotenv>=1.0.0
PyJWT>=2.8.0
openai>=1.6.0
anthropic>=0.18.0
sse-starlette>=1.8.0
auth0-python>=4.0.0

# Optional — install for full integration:
# airbyte>=0.10.0         # PyAirbyte dynamic connectors
# overmind>=0.1.0          # Overmind SDK auto-instrumentation
# overclaw                 # OverClaw agent optimizer (install via: uv tool install overclaw)
